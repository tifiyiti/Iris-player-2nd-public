/// Settings page grouping. Mirrors the legacy tab layout so the metadata
/// renderer reproduces the old information architecture one-for-one.
enum SettingsSection {
  /// Playback behaviors (legacy Play tab).
  play,

  /// App-wide preferences (legacy General tab).
  general,
}
