enum BrowserOpenMode {
  /// Default: the storagedb shows the tab host at [lastActiveTab].
  tabs,
  storageFiles,
  mock,
  libContent,
  scenario,

  /// The independent media search page (media-library entry, F-001).
  search,
}
